{
    "name": "portfolio",
    "version": "1.0.0",
    "scripts": {
    "build-and-start": "npm run build && cd backend && npm install && npm start",
    "build": "cd frontend && npm install && npm run build"
  }
}